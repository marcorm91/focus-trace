# Audit export format

Multipage audit PDF export uses one cover and one section per normalized page URL. Every page section shows the timestamp of the latest full-page review and the latest saved static findings. Repeated scans of the same URL replace that section instead of adding another copy.
