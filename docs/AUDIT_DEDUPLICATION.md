# Audit deduplication

The audit page key is the normalized page URL. Ordinary hash anchors are ignored while query strings and SPA route hashes are preserved. Updating the same key replaces the previous page record.
