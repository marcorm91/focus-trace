# Audit model

An audit contains one latest snapshot per normalized page URL. The page's `scannedAt` value is the review timestamp shown in UI/PDF. The active audit can include more than one site only after an explicit user choice when a new site is encountered.
