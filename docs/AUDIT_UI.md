# Audit UI

The Report workspace shows the active audit summary and the current page report. When the current page has not been analyzed, Report uses the same centered empty-state pattern as Structure while keeping any existing audit summary/export available.
