# Audit print entrypoint

This WXT page renders the active multipage audit as one printable document. It consumes a one-time `browser.storage.session` evidence token created by the sidepanel and does not fetch or rescan audited pages during export.
